This Lambda function is triggered by S3 ObjectCreated on failed-events/. It aggregates every incidents and after that generates summary JSON and HTML dashboard.
# Note
Every sensitive data was hidden in this file.